name: Update Pages

on:
  push:
    branches: [master]

jobs:
  update_pages:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm ci
      - run: npm run build-storybook
